{
  "name": "blockchain",
  "version": "1.0.0",
  "description": "TrustLayer + JobLedger — Hardhat ya Truffle se Ganache par deploy.",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:ganache:hh": "hardhat run scripts/deploy.js --network ganache",
    "compile:truffle": "npx --yes truffle@5.11.5 compile",
    "migrate:ganache": "npx --yes truffle@5.11.5 migrate --network development --reset",
    "test": "echo \"Run: npx hardhat test\" && exit 0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^7.0.0",
    "hardhat": "^3.2.0"
  }
}
